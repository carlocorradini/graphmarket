/**
 * Common environment that must be present in dev and prod environments.
 */
export const environment = {
  // Application name
  appName: 'Graph Market',
  // GraphQL URI
  apiURI: 'http://localhost:8080/graphql',
};
